---

- hosts: all
  become: true
  tasks:

  - name: install apache2 package
    apt:
      name: apache2
      state: latest

  - name: update cache for installed packages
    apt:
      update_cache: true
